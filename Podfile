# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OneExplorer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OneExplorer

  target 'OneExplorerTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'SwiftyJSON', '~> 2.3'
  end

  target 'OneExplorerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
